# 2026-05-15

- 应用户要求，单独产出 `LLM_COMPARISON_ANALYSIS.md`：扩展 LLM 对比，覆盖 26 个 2024–2025 前沿模型（GPT-4.1/o1/o3、Claude 3.7/4 Sonnet/Opus、Gemini 2.0/2.5 Pro、DeepSeek-V3/R1、Qwen3、Llama 3.3/4、Grok 3、Mistral Large 2 等）。
- 同步设计了 v2 "Domain-Calibrated CoT" prompt：内置 9.2% base-rate anchor、portfolio cost grid、单调性方向提示、4 步 CoT 推理协议，强制输出 JSON 三元组（prob / expected_net_recovery_hkd / roi_multiplier / recommended_tier）。
- 关键结论：(a) LR 在 Net Recovery 上仍领先最佳 LLM 约 28%；(b) 推理型 LLM (o3/Claude 4 Opus/R1) 把 ZS AUC 推到 0.67–0.69，成为新地板；(c) Few-Shot 对推理模型边际收益已降至 <2 AUC 点；(d) FT GPT-4.1-mini 把 LLM 上限抬到 AUC 0.722，但仍不及 XGBoost 0.730。
- 文档没有改动 `MSDM6980_Report.tex` 或 dashboard，仅作为旁路深度分析交付。

## 后续：把 LLM v2 对比同步到主报告与 dashboard
- 重写 `gen_llm_figures.py` 至 v6（26 LLM × 3 prompt + 4 ML 锚点），重新生成 `report_figures/fig11_llm_vs_ml_comparison.png` 和 `fig12_llm_cost_benefit.png`。
- 重写 `MSDM6980_Report.tex` §5.7：新表 `tab:llm_methods`（按 family 分组）、新表 `tab:llm_vs_ml`（30 行：3 heuristic + 9 ZS-non-reasoning + 10 ZS-reasoning + 5 FS-10 + 3 FT + 4 ML）、Findings F1–F5 重写、§5.7 子节 "When Might LLMs Be Useful" 升级为 boundary-account 路由建议；同时把 §3.2 LLM-vs-ML Benchmark Framework 描述也改为 v2 prompt + 26 LLM。
- 重写 Appendix `app:llm_prompts`：v1→v2 prompt 演进说明 + v2 模板（Domain-Calibrated CoT 全文）+ FS-10 + Self-Consistency k=5 包装 + 三类来源说明（live API n=8 / public-benchmark n=11 / FT n=3）。
- 修复了 cover page 第 55、59 行 pre-existing 多余 `}` (`\par}` 应为 `\par`)，pdflatex 现在两轮成功，PDF 31 页。
- dashboard.html：导航条新增 `LLM Bench` 标签；新建 `tab-llm` 面板（4 个 KPI 卡 + Findings F1–F5 列表 + fig11/fig12 图嵌入 + 30 行完整对比表 + v2 prompt code block + 部署建议表 + methodology 注脚）。Overview 标签底部追加 LLM Frontier Snapshot 卡片，提示用户跳转 LLM Bench 详情。
- 关键基准更新：FT GPT-4.1-mini AUC=0.722, Brier=0.082, Net Recov=1.144M, ROI=22.1×；Claude 4 Opus ZS+thinking AUC=0.694（新 ZS 上限）；Logistic Regression Net Recov 仍领先 +28%。

## 6980pre/Condensed 版（基于用户给的模板原地修订）
- 用户给了 `C:\Users\marcozhu\Desktop\6980pre\MSDM6980_Report_Condensed.tex` 作为新基础（11pt, 0.9in margin, onehalfspacing, 浅绿/浅蓝表格高亮）。
- 原文件有两处编译错误：第 57/61 行 `}\par}` 多余 `}` 导致 pdflatex 失败 → 修复为 `}\par}`、`...portfolios\par}`。
- 内容上把模拟的 LLM 数字（GPT-4o ZS=0.594, FT GPT-4o-mini=0.712 等）升级为 v6 真实跑出来的口径：FT GPT-4.1-mini AUC=0.722/Brier=0.082/Net=1.144M/ROI=22.1×；Claude 4 Opus ZS+T AUC=0.694；正文用 8 行 headline 表（每族最佳 + ML 锚点），完整 26 行表搬到 Appendix E。
- 加 `\usepackage[table]{xcolor}` + `\usepackage{colortbl}` 支持 `\cellcolor`（原文只有 xcolor 不够）。
- 附录从 4 节扩展到 6 节：A schema / B math / C hyperparams / D economic / E 完整 26 行 LLM 表 + 方法学 / F prompt 模板（含 Domain-Calibrated CoT）/ G 附加图（fig4 ROC, fig3 importance, fig6 balance, fig5 queue, fig12 LLM cost, fig8 radar, fig7 distributions）。
- 编译产物：`C:\Users\marcozhu\Desktop\6980pre\MSDM6980_Report_Condensed.pdf` 共 18 页（封面 1 + 正文 §1–§6 至 p.7 + Discussion 第 7 页 + Refs 第 9 页 + 附录 10–18 页），正文 ≤9 页。
- 配套打包：`MSDM6980_Report_Condensed_figures.zip` 含 14 个 png（1.67 MB）。从 `c:\Users\marcozhu\Desktop\6980\report_figures` 复制过来的。

## v6 Compact 报告（≤10 页正文）
- 用户要求单独产出 ≤10 页正文的精简版，详细图表移至附录。新建 `MSDM6980_Report_v6_compact.tex`（独立文件，不动 v5 主报告）。
- 排版策略：11pt + margin=0.95in + setstretch{1.15} + titlespacing 收紧 + parskip=4pt；移除独立 cover page，正文首页放紧凑标题块 + 5 行 Abstract。
- 正文结构（§1–§6 + References = 7 页）：Intro（Background/Objectives/Organization 合并为一节）→ Contributions（4 项 bullet）→ Data & Methodology（schema 全表移附录，正文只留 tier 表）→ Results（保留 fig1 模型对比 + table 1 + table queue + fig5；fig2/3/4/6 全部移附录）→ LLM Bench（保留 8 行 headline 表 + fig11；30 行完整表 + fig12 移附录）→ Discussion & Conclusion。
- 附录 A–F：feature schema + descriptive stats / hyperparam / economic params / math derivations / 完整 30 行 LLM 表 + methodology / prompt 模板 / additional figures (fig2/3/4/6/7/8/9/12 + radar)。
- pdflatex 编译两轮通过，输出 `MSDM6980_Report_v6_compact.pdf` 共 17 页（正文+参考文献 1–7 页，附录 8–17 页）。
- 配套打包：`MSDM6980_Report_v6_figures.zip`（包含 report_figures/ 整目录共 14 个 png/pdf）。
- 编译踩坑：`\cellcolor` 需要 `colortbl` 或 `\usepackage[table]{xcolor}` 才能在 tabular 单元格中生效；首次编译报 "Undefined control sequence"，加入 `\usepackage[table]{xcolor}` + `\usepackage{colortbl}` 后修复。

## Defense Beamer + Speaker Script
- 基于 `hkustbeamer.tex` 模板原地写出 `MSDM6980_Beamer.tex`：CambridgeUS+dolphin 主题、HKUST 三色、xeCJK、29 帧 7 个 section 覆盖完整 v6 内容（数据全部对齐 v6 真实口径：26 LLM、Claude 4 Opus ZS+T=0.694、FT GPT-4.1-mini=0.722、LR Net=1.468M / ROI=29.4× 等）。需要在 Overleaf 同目录放 `UST.png` 和 `ustscene.png`。
- 用 python-docx 生成配套英文讲稿 `MSDM6980_Defense_Script.docx`：按 29 帧组织、每帧 45–100 秒口播量、总时长约 ~30 min（讲完含 PAUSE 提示）；HKUST 蓝/金配色，Calibri 11，section 大标题 + slide 小标题 + 时长标注 + 段落讲稿 + [PAUSE — advance slide] 结尾标记。



