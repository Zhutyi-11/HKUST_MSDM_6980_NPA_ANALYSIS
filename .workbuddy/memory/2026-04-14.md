# 2026-04-14

- **Dashboard v7 完全重写**（从零构建，非修补）：彻底解决了 v6 所有交互失效问题。
- **v8 最终版**：修复 6 类问题，输出 456.2 KB，8 个 Tab。
- **Dashboard v9 完全重写**（用户要求"按钮点不了、更多模型对比、优化UI、整理文件"）：
  - **按钮交互修复**：全部改用 `data-*` 属性 + `addEventListener` 绑定（不再用内联 onclick）。Tab 切换用 `data-tab`，详情展开用 `data-detail-btn`，账户查看用 `data-acct-id`。所有事件在 DOMContentLoaded 中统一注册。
  - **新增 Compare Tab（模型对比扩展）**：
    - 雷达图（AUC/1-Brier/Recall/Precision/ROI/NetRev 六维归一化）
    - AUC vs Brier 散点图（每个模型一个点）
    - 阈值敏感性曲线（模拟不同 threshold 下各模型 ROI）
    - Brier Score 水平柱状图对比
    - Precision vs Recall 散点图
    - 模型决策矩阵表（每行一个指标，高亮最优模型）
    - Selection Verdict 总结卡片
  - **UI 美化**：
    - 深色渐变主题（radial-gradient 背景 + 卡片 gradient）
    - KPI 渐变顶部指示条（CSS `--kpi-accent` 变量）
    - fadeIn 动画（Tab 切换）、hover 上浮效果
    - Chart.js 渐变填充柱状图（直方图）
    - 粘性表头（thead th sticky）
    - 响应式布局优化（移动端适配）
    - glow-green/glow-blue 文字发光效果
  - **工作区清理**：删除 25 个 `_*.py` 临时调试脚本（释放 107 KB）
  - 输出 **487 KB / 9 个 Tab**
  - 技术债务：report_html 注入改用 `json.dumps(report_md)` 彻底解决引号转义问题
