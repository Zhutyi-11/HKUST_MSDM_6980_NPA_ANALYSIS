# 2026-04-14

- **Dashboard v7 完全重写**（从零构建，非修补）：彻底解决了 v6 所有交互失效问题。v6 的根本原因是 HTML 被压缩成单行 + JS 语法错误导致浏览器无法解析。
- **v7 架构改进**：
  1. Python 生成脚本结构清晰，数据预处理与 HTML 生成分离
  2. JS 使用 `data-col` 属性 + `addEventListener` 绑定排序（不再用内联 onclick 混淆）
  3. 图表按 Tab 延迟初始化（`initChartsForTab`），避免不可见图表的渲染错误
  4. 数据统一注入为 JS 全局变量：ACCOUNTS/QUEUE_DATA/DEV_SPLIT/CM_VALUES/MODEL_DATA/ALL_FI 等
  5. Modal 包含风险分层标签 + AI 解读文案
  6. 删除所有代理对 emoji（surrogate pair），改用 ASCII 文本
- **修复的具体 bug**：(1) 缩进错误 (2) Unicode surrogate 编码错误 (3) 非数字分类变量值转 float 错误 (4) JS 引号未闭合
- **输出**：208.9 KB，7 个 Tab 全部可用，所有按钮/图表/筛选/排序/导出/Modal 正常工作。
- **Dashboard v6**（301KB）是中间版本，已废弃。
