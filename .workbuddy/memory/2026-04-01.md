# 2026-04-01

- 为投产版 `npa_repayment_agent` 新增显式基线模型 `baseline_logistic_regression`，并把 Agent Champion 与基线的 T 集性能对比写入报告和 `agent_vs_baseline_summary.csv`。在 `data.xlsx` 上复跑后，Champion 仍为 `xgboost`：相对基线 ROC-AUC +0.086、Brier -0.0032、LogLoss -0.0172、Precision +4.30pct、预期净回收代理值 +316,416、ROI +3.79x；但 Recall 低 5.45pct，说明业务上是在少量召回换取更高精度和更高经济价值。
- 基于 baseline_comparison_run 全部产物生成交互式 Dashboard（`dashboard.html`），支持5个Tab（模型比较/生产队列/账户明细/特征重要性/组合信号），含表格排序、多维度筛选（ID搜索/队列/付款标记/贷款类型）、账户级下钻模态框与AI数据解读面板；使用暗色Glass Morphism风格+Chart.js可视化。生成脚本位于 `.workbuddy/skills/npa-repayment-agent/scripts/generate_dashboard.py`。
