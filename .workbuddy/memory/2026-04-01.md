# 2026-04-01 ~ 04-13

- 为投产版 `npa_repayment_agent` 新增显式基线模型 `baseline_logistic_regression`，并把 Agent Champion 与基线的 T 集性能对比写入报告和 `agent_vs_baseline_summary.csv`。
- **v3 新增深度学习 MLP 模型**（PyTorch 3层全连接 + BatchNorm + Dropout + AdamW），Champion 从 xgboost 变为 deep_mlp：T集净回收 ¥3,205,745（+¥946,397 vs 基线），ROI 38.44x（+11.35x vs 基线），Recall 95.64%。MLP 的 AUC（0.677）低于 XGBoost 但经济指标更优——验证了"AUC ≠ 业务价值"。
- Dashboard v3→v4: 彻底重写生成脚本，核心修复：(1) 模型名映射表完整覆盖 deep_mlp/deep_champion 等新角色（不再显示unknown）；(2) KPI/表格数据字段与metrics.json完全对齐（不再显示0）；(3) 账户模态框详情完整展示所有字段+AI解读+各渠道回收拆解；(4) Tab按钮用data-tab属性+onclick正确绑定；(5) 表格排序、筛选、导出CSV全部可点击；(6) 模型行展开显示完整指标卡片含混淆矩阵和经济测算。
- **MLP v1→v2 优化完成**：核心改进包括 (a) Swish 替代 ReLU + 残差连接(1块) + 特征交互层(64d)；(b) OneCycleLR 学习率调度(先升后降)；(c) 标签平滑(label_smoothing=0.03)；(d) 梯度裁剪(grad_clip_norm=3.0)；(e) 手动样本加权 BCELoss。效果：AUC 0.677→**0.709**(追平XGBoost)，Brier 0.0855→**0.0799**，LogLoss 0.3049→**0.2888**；Recall 从95.6%降至83.4%（更合理的预测分布），净回收从¥320万降至¥292万但 ROI仍达35x。Champion 仍为 deep_mlp。
- 重写研究报告为完整学术/业务报告格式：数据简介(16k样本15字段)→预处理管线→4种建模方法详解(LR/RF/XGB/MLP含架构图)→实验结果对比→可视化图表解读(5类)→生产队列策略矩阵→结论与局限。
- README 已更新为完整版，覆盖全部文件说明和复现步骤。
- **Dashboard v5 生成修复**：修复了 generate_dashboard.py 中 4 个 bug：(1) 字段名 open_clocking_m→open_closing_m；(2) 第 208 行引号转义冲突（\' 改 &apos;）；(3) 第 305 行引号嵌套未闭合，改用列表拼接方式；(4) json.stringify→json.dumps + CSV int 转换。输出 153KB HTML 含 7 个 Tab（Overview/Models/Features/Stats/Queue/Tuning/Report）。
